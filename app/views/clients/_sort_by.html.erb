<span style="display: inline-block;">
  <%= form_tag url_for(action: 'sort_options'), class: 'select-form-styler' do %>
      <%=label_tag :selected, 'Сортировать по: '%>
      <%=  select_tag :selected, options_for_select(@options), prompt: "#{@prompt}", onchange: "this.form.submit()", id: 'sorter'%>
  <% end %>
</span>

