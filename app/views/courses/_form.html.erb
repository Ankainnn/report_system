<div class="form-wrap clearfix">
  <h1>Новый курс</h1>
  <hr>
  <%= flash[:notice]%>
<%= simple_form_for(@course) do |f| %>
    <%= f.input :direction, label: "направление" %>
    <%= f.input :subject, label: "предмет" %>
    <div id="submit">
      <%= f.button :submit, :value => "сохранить"%>
      <div>
        <%= link_to 'к списку', courses_path, class: "btn"%>
      </div>
    </div>
<% end %>
</div>