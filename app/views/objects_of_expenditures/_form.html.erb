<div class="form-wrap clearfix">
  <h1><%= action_name == 'edit' ? 'Редактирование объекта расходов' : 'Новый объект расходов'%> </h1>
  <hr>
  <%= flash[:notice]%>
<%= simple_form_for @objects_of_expenditure, :html => { :class => 'form-horizontal' } do |f| %>

    <%= f.input :name, label: "Название", :input_html => { :cols => 50, :rows => 4 } %>
    <div id="submit">
      <%= f.button :submit, :value => "Сохранить"%>
      <div>
        <%= link_to 'К списку', objects_of_expenditures_path, class: "btn"%>
            </div>
            </div>
        <% end %>

</div>