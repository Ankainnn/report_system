<%= simple_form_for(@payment) do |f| %>
  <%= f.input :date %>
  <%= f.input :summ %>
  <%= f.input :type, :collection => ["Наличные", "По безналу"], :as => :radio_buttons %>
  <%= f.association :order %>
  <%= f.button :submit %>

<% end %>
