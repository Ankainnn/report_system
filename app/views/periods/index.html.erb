<h1>Периоды курсов</h1>

<table>
  <tr>
    <th>периоды</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @periods.each do |period| %>
  <tr>
    <td><%= period.period %></td>
    <td><%= link_to 'см.', period %></td>
    <td><%= link_to 'ред.', edit_period_path(period) %></td>
    <td><%= link_to 'уд.', period, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Period', new_period_path %>
