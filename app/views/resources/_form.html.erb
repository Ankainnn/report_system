<%= simple_form_for(@resource) do |f| %>

    <%= f.input :name, label: "название" %>
    <div id="submit">
      <%= f.button :submit, :value => "сохранить"%>
      <div>
        <%= link_to 'к списку', resources_path, class: "btn"%>
      </div>
    </div>

<% end %>
