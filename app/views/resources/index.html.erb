<h1>Районы</h1>

<%= link_to 'новый район', new_resource_path %> | <%= link_to 'загрузить таблицу в XLS формате', district_to_xls_path + ".xls" %>
<table id="demotable" class="table table-striped">
  <thead>
  <tr class="sort">
    <th>название</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
<% @resources.each do |resource| %>
  <tr>
    <td><%= resource.name %></td>
    <td><%= link_to 'см.', resource %></td>
    <td><%= link_to 'ред.', edit_resource_path(resource) %></td>
    <td><%= link_to 'уд.', resource, confirm: 'Вы уверены?', method: :delete %></td>
  </tr>
<% end %>
  </tbody>
</table>

