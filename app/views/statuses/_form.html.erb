<%= simple_form_for(@status) do |f| %>
    <%= f.input :name, label: "имя" %>
    <div id="submit">
      <%= f.button :submit, :value => "сохранить"%>
      <div>
        <%= link_to 'к списку', statuses_path, class: "btn"%>
      </div>
    </div>
<% end %>
