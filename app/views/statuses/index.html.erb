<h1>Справочник статусов</h1>

<%= link_to 'новый статус', new_status_path %> | <%= link_to 'загрузить таблицу в XLS формате', statuses_to_xls_path + ".xls" %>
<table id="demotable" class="table table-striped">
  <thead>
  <tr class="sort">
    <th>название</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
 </thead>
  <tbody>
<% @statuses.each do |status| %>
  <tr>
    <td><%= status.name %></td>
    <td><%= link_to 'см.', status %></td>
    <td><%= link_to 'ред.', edit_status_path(status) %></td>
    <td><%= link_to 'уд.', status, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
  </tbody>
</table>

