<h1>Пользователи</h1>
<%= link_to "Добавить нового пользователя", add_user_path%>

<table class="table table-striped">
  <tr>
    <th>email</th>
    <th>active</th>
    <th></th>

  </tr>

<% @users.each do |course| %>
  <tr>
    <td><%= course.email %></td>
    <td><%= course.active %></td>
    <td><%= link_to 'Activate', change_user_user_path(course.id) %></td>

  </tr>
<% end %>
</table>